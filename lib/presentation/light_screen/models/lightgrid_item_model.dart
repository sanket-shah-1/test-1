/// This class is used in the [lightgrid_item_widget] screen.

// ignore_for_file: must_be_immutable
class LightgridItemModel {LightgridItemModel({this.frontDoor, this.id}) { frontDoor = frontDoor ?? "Front Door"; id = id ?? ""; }

String? frontDoor;

String? id;

 }
