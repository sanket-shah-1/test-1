/// This class is used in the [roomsviewall_item_widget] screen.

// ignore_for_file: must_be_immutable
class RoomsviewallItemModel {RoomsviewallItemModel({this.livingRoom, this.activeCounter, this.twentySix, this.c, this.id}) { livingRoom = livingRoom ?? "Living Room"; activeCounter = activeCounter ?? "2 active"; twentySix = twentySix ?? "26"; c = c ?? "C"; id = id ?? ""; }

String? livingRoom;

String? activeCounter;

String? twentySix;

String? c;

String? id;

 }
